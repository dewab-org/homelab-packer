# Base template 9228 is bootstrapped from the upstream qcow2 by
# builds/linux/common/scripts/bootstrap-cloud-bases.py (see
# builds/linux/common/cloud-base-images.json for the pinned image + sha256).
clone_vm_id   = 9228
vm_id         = 9238
template_name = "rocky-8-cloud"
tags          = "rocky;cloud-init;packer"
