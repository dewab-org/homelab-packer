# Windows Server 2022 (stub)

This directory is a stub for a future Windows Server 2022 build.

Required outcomes:
- Unattend-based install
- QEMU/virtio drivers injected
- Cloudbase-Init (or equivalent) prepared
