title = "homelab gitleaks config"

[allowlist]
description = "Ignore local environment files and generated local state"
paths = [
  '''(^|/)\.env(rc)?$''',
  '''(^|/)\.env\..*$''',
  '''(^|/)\.venv/''',
  '''(^|/)\.work/''',
]
